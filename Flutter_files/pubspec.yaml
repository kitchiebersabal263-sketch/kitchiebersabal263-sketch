name: bersabal_flutter
description: "Advanced AI-powered knife detection system"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.2.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  image_picker: ^1.0.4
  tflite_flutter: ^0.11.0
  image: ^4.1.3
  fl_chart: ^0.68.0
  google_fonts: ^6.1.0
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/model_unquant.tflite
    - assets/labels.txt
